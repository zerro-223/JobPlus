<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobplus.mapper.PositionMapper">

    <!-- 动态搜索 -->
    <select id="search" parameterType="com.jobplus.common.dto.PositionQuery"
            resultType="com.jobplus.entity.Position">
        SELECT p.* FROM position p
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%')
                 OR p.description LIKE CONCAT('%', #{keyword}, '%')
                 OR p.tags LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="city != null and city != ''">
            AND p.workplace LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="salaryMin != null">
            AND p.salary_max >= #{salaryMin}
        </if>
        <if test="education != null and education != ''">
            AND p.education = #{education}
        </if>
        <if test="categoryId != null">
            AND p.category_id = #{categoryId}
        </if>
        <if test="status != null">
            AND p.status = #{status}
        </if>
        <if test="companyId != null">
            AND p.company_id = #{companyId}
        </if>
        ORDER BY p.created_at DESC
        <if test="page != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <!-- 搜索结果计数 -->
    <select id="count" parameterType="com.jobplus.common.dto.PositionQuery"
            resultType="long">
        SELECT COUNT(*) FROM position p
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%')
                 OR p.description LIKE CONCAT('%', #{keyword}, '%')
                 OR p.tags LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="city != null and city != ''">
            AND p.workplace LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="salaryMin != null">
            AND p.salary_max >= #{salaryMin}
        </if>
        <if test="education != null and education != ''">
            AND p.education = #{education}
        </if>
        <if test="categoryId != null">
            AND p.category_id = #{categoryId}
        </if>
        <if test="status != null">
            AND p.status = #{status}
        </if>
        <if test="companyId != null">
            AND p.company_id = #{companyId}
        </if>
    </select>

</mapper>
